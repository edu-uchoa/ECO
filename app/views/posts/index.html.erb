<div class="space-y-8">
  <div class="flex items-center justify-between">
    <h1 class="text-3xl font-bold">Itens para Doação</h1>
  </div>

  <div class="flex flex-col gap-4 md:flex-row md:gap-2">
    <%= form_with url: posts_path, method: :get, local: true, class: "flex flex-col gap-2 md:flex-row" do |f| %>
      <div class="flex-1">
        <%= f.select :category, options_for_select([""] + Post::CATEGORIES, params[:category]), { prompt: "Todas as categorias" }, class: "rounded-lg border-gray-300 px-4 py-2 border shadow-sm" %>
      </div>
      <div class="flex-1">
        <%= f.text_field :location, placeholder: "Filtrar por localização", value: params[:location], class: "rounded-lg border-gray-300 px-4 py-2 border shadow-sm" %>
      </div>
      <div class="flex gap-2">
        <%= f.submit "Filtrar", class: "rounded-lg bg-blue-600 px-4 py-2 font-semibold text-white shadow-sm hover:bg-blue-500" %>
        <%= link_to "Limpar", posts_path, class: "rounded-lg bg-gray-300 px-4 py-2 font-semibold text-gray-900 shadow-sm hover:bg-gray-400" %>
      </div>
    <% end %>
  </div>

  <% if @posts.any? %>
    <div class="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">
      <% @posts.each do |post| %>
        <div class="overflow-hidden rounded-lg border border-gray-200 shadow-sm hover:shadow-md transition-shadow">
          <% if post.images.attached? %>
            <div class="h-48 overflow-hidden bg-gray-100">
              <%= image_tag post.images.first, alt: post.title, class: "h-full w-full object-cover" %>
            </div>
          <% else %>
            <div class="h-48 bg-gray-200 flex items-center justify-center">
              <span class="text-gray-400">Sem imagem</span>
            </div>
          <% end %>

          <div class="p-4 space-y-3">
            <div>
              <h3 class="font-bold text-lg text-gray-900"><%= post.title %></h3>
              <%= link_to post.user.name, user_path(post.user), class: "text-sm text-blue-600 hover:text-blue-800 hover:underline" %>
            </div>

            <p class="line-clamp-2 text-sm text-gray-700"><%= post.description %></p>

            <div class="flex flex-wrap gap-2">
              <span class="inline-block rounded-full bg-blue-100 px-3 py-1 text-xs font-medium text-blue-700">
                <%= post.category %>
              </span>
              <span class="inline-block rounded-full bg-green-100 px-3 py-1 text-xs font-medium text-green-700">
                <%= post.condition %>
              </span>
            </div>

            <div class="text-sm text-gray-600">
              📍 <%= post.location %>
            </div>

            <%= link_to "Ver Detalhes", post_path(post), class: "inline-block rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-blue-500 w-full text-center" %>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="rounded-lg border-2 border-dashed border-gray-300 p-12 text-center">
      <p class="text-gray-600">Nenhum item encontrado</p>
    </div>
  <% end %>
</div>

<div class="mb-4">
  <%= js_back_button "Back", class: "px-3 py-2 rounded bg-gray-100 text-sm" %>
</div>
